{
    "name": "@pexip/poll-plugin",
    "version": "0.1.0",
    "private": true,
    "description": "Demo for creating polls in app through a plugin",
    "license": "UNLICENSED",
    "homepage": "https://gitlab.com/pexip/zoo",
    "bugs": "https://gitlab.com/pexip/zoo/issues",
    "repository": {
        "type": "git",
        "url": "https://gitlab.com/pexip/zoo.git",
        "directory": "src/aquila/examples/poll-plugin"
    },
    "packageManager": "yarn@1.22.21",
    "scripts": {
        "build": "vite build",
        "clean": "rm -rf dist",
        "start": "vite",
        "check-format": "prettier --ignore-path=../../.prettierignore --check .",
        "format": "prettier --ignore-path=../../.prettierignore --write .",
        "lint": "yarn run -T eslint --ext .js,.ts,.tsx --report-unused-disable-directives .",
        "typecheck": "yarn tsc --noEmit -p ."
    },
    "devDependencies": {
        "prettier": "^3.0.3",
        "typescript": "~5.3.0",
        "vite": "^4.5.1"
    },
    "dependencies": {
        "@pexip/plugin-api": "workspace:*"
    }

}
